APP_NAME="${KUBE_APP_NAME}"
APP_ENV="${KUBE_APP_ENV}"
APP_KEY="${KUBE_APP_KEY}"
APP_DEBUG="${KUBE_APP_DEBUG}"
APP_URL="${KUBE_APP_URL}"
#ENDERECO_SSOWS_LOGIN="${KUBE_ENDERECO_SSOWS_LOGIN}"
#ENDERECO_SSOWS_ACAO="${KUBE_ENDERECO_SSOWS_ACAO}"

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION="${KUBE_DB_CONNECTION}"
DB_HOST="${KUBE_DB_HOST}"
DB_PORT="${KUBE_DB_PORT}"
DB_DATABASE="${KUBE_DB_DATABASE}"
DB_USERNAME="${KUBE_DB_USERNAME}"
DB_PASSWORD="${KUBE_DB_PASSWORD}"

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=null
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

SSO_URL="${KUBE_SSO_URL}"
RELOAD_SISTEMA="${KUBE_RELOAD_SISTEMA}"
ENDERECO_LEGADOWS="${KUBE_ENDERECO_LEGADOWS}"

WWWGROUP=1000
WWWUSER=1000
